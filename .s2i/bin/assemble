#!/bin/bash

set -e

shopt -s dotglob
echo "---> Installing application source..."
mv /tmp/src/* ./

# Install Composer
curl https://getcomposer.org/installer | php
make COMPOSER_CMD=`pwd`/composer.phar build

# Fix source directory permissions
fix-permissions ./
